import { FilterWithTextPipe } from './filter-with-text-pipe';

describe('FilterWithTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWithTextPipe();
    expect(pipe).toBeTruthy();
  });
});
